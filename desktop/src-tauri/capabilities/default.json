{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the SMDL desktop main window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
